<?xml version="1.0" encoding="utf-8" ?>
<Page
    x:Class="Klicka.TimingPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="using:Klicka"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}"
    NavigationCacheMode="Enabled"
    mc:Ignorable="d">

    <RadioButtons
        x:Name="intervalBtns"
        Header="Input Interval"
        SelectedIndex="0">
        <RadioButton x:Name="rb1">
            <NumberBox
                x:Name="intervalInputBox"
                Header="Interval in ms"
                LargeChange="100"
                SmallChange="10"
                SpinButtonPlacementMode="Inline"
                Value="100" />
        </RadioButton>
        <!--<RadioButton x:Name="rb2">
    <StackPanel Orientation="Horizontal">
        <NumberBox
            x:Name="randIntervalStartInputBox"
            Header="Start"
            LargeChange="100"
            SmallChange="10"
            SpinButtonPlacementMode="Inline"
            Value="100" />
        <NumberBox
            x:Name="randIntervalEndInputBox"
            Header="End"
            LargeChange="100"
            SmallChange="10"
            SpinButtonPlacementMode="Inline"
            Value="100" />
    </StackPanel>
</RadioButton>-->
    </RadioButtons>
</Page>
